# Review

Review the frontend codebase (or a specific feature if provided) against project guidelines.

The scope is provided as: $ARGUMENTS

If $ARGUMENTS is empty, review the full project.
If $ARGUMENTS is a domain name (e.g., `users`, `billing`), review files related to that domain only.

Spawn the `vue-reviewer` agent with the appropriate scope.
